GOLDEN FIXTURE (secrets-backed rules) — synthetic values only.
These rules carry no regex in risk-patterns.yaml; they only fire when the
operator supplies literals via --secrets-file. Paired with
tests/fixtures/secrets-example.secrets, sanitize-lint must exit non-zero here.

# person_name (supplied as a literal)
report author: Casey Doe

# company_domain (supplied as a regex)
internal git host: git.acme-internal.test
project code name: acmeproj-backoffice
